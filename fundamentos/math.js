const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2) // raio elevado ao quadrado
//Math.PI = numero PI
//Math.pow = potenciação

console.log(area);
console.log(typeof Math); // Math é um objeto (usamos uma constante 'PI' e uma função 'pow')
